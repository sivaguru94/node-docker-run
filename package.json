{
  "name": "docker-run",
  "version": "1.0.0",
  "description": "Node program to run any docker yml files in the docker-compose folder",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sivaguru Ravi",
  "license": "ISC",
  "dependencies": {
    "yargs": "13.2"
  }
}
